<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <import type="android.view.View" />

        <variable
            name="checkedTag"
            type="Boolean" />

        <variable
            name="tag"
            type="com.pasich.mynotes.data.database.model.Tag" />

    </data>

    <FrameLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="5dp"
        android:background="@{checkedTag ? @drawable/item_tag_checked : @drawable/selector_item_tab_unchecked}"
        android:gravity="center"
        android:orientation="vertical">


        <com.google.android.material.textview.MaterialTextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="@{tag.systemAction == 1 ? View.VISIBLE : View.GONE}"
            android:textColor="?attr/colorPrimary"
            android:textSize="20sp"
            android:contentDescription="@string/addTag"
            android:text="@string/plus"
            android:layout_marginVertical="11dp"
            android:layout_marginHorizontal="15dp" />

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/nameTag"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{tag.systemAction == 2 ?  @string/allNotes :  tag.nameTag}"
            android:textColor="?colorOnSurfaceVariant"
            android:textStyle="bold"
            android:layout_margin="15dp"
            android:visibility="@{tag.systemAction != 1 ? View.VISIBLE : View.GONE}" />
    </FrameLayout>
</layout>